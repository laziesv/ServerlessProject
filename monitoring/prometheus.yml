global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: todo-list
    metrics_path: /metrics
    static_configs:
      - targets:
          - todo-list-service.todo-list.svc.cluster.local:3000
          - host.docker.internal:3000
